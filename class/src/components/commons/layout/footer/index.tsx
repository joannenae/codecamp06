import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 50px;
  background-color: lightgray;
`;

export default function Layoutfooter() {
  return <Wrapper>여기는 푸터 영역입니다.</Wrapper>;
}
